wizzlern_crawler.endpoint.*:
  type: config_entity
  label: 'Endpoint config'
  mapping:
    id:
      type: string
      label: 'ID'
    label:
      type: label
      label: 'Label'
    uuid:
      type: string
    endpoint_url:
      type: string
      label: 'URL'
    processors:
      type: array
      label: 'Processors'
